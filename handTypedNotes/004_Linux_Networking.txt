| OSI Layer    | Technical Function                              
| ------------ | ------------------------------------------------|
| Application  | User-level services / app protocols             |
| Presentation | Data translation, encryption, compression       |
| Session      | Session establishment, maintenance, termination |
| Transport    | Reliable delivery, segmentation, error checking |-> tcp(slow but transper the full data used in ex;- mail), udp(fast but transfer not full ex; games data)
| Network      | Routing, addressing, packet forwarding          |
| Data Link    | Local delivery, error detection, MAC addressing |->Check the complete data transfer if anything is missing or what b/w "nw n ph"
| Physical     | Physical signal transmission                    |


+----------------+---------------------------------------+-------------------------------------------------------------+----------------------+
| OSI Layer      | What It Does                          | What Happens to "hello world"                               | PDU (Data Unit)      
+----------------+---------------------------------------+-------------------------------------------------------------+----------------------+
| 7. Application | User interacts with software.         | You type the command in your terminal/SSH.                  | Data 
|
| 6. Presentation| Encoding, encryption, formatting.     | Text → UTF-8 and encrypted in SSH.                          | Data 
|
| 5. Session     | Maintains login/session.              | Your SSH session manages the terminal link.                 | Data 
|
| 4. Transport   | Reliable delivery (TCP).              | Data is split into TCP segments and delivered reliably.     | Segment
|
| 3. Network     | Routing using IP addresses.           | Adds Source & Destination IP for routing.                   | Packet 
|
| 2. Data Link   | Uses MAC addressing (LAN/Wi-Fi).      | Frames are created with Source & Destination MAC.           | Frame  
|
| 1. Physical    | Sends raw bits over the medium.       | Bits travel as electrical signals or radio waves.           | Bits                 
+----------------+---------------------------------------+-------------------------------------------------------------+----------------------+

All People Seem To Need Data Processing 
A  P   S   T  N   D   P
7  6   5   4  3   2   1

Do Some People Fear Birthdays?
Data → Segment → Packet → Frame → Bits


Networking cmds

1. IP CMDS- ip addr show 
2. ifconfig
3. ping -> website/vm -> just to check or confirm its up and running
4. traceroute google.com -> trace the route that is hop along with time its taken 
5. netstat -tuln -> protocall has been used
6. ss -tuln (alternative to netstat -tuln ) which will gives the protocalls in detials list
7. route -n ->  gives the details of the data packates are travelling.
8. dig google.com -> cmd domain name to ip address
9. nslookup google.com-> alternative to dig cmd as nslookup shows the easy readable format that domains ip adress.
10. sudo iptable -A INPUT -p tcp --dport 22 -j ACCEPT -> accept the data from this 
11. sudo iptable -A INPUT -p tcp --dport 22 -j DROP -> will drop.
12. hostname
13. telnet ip 22 -> check the vm enabled the port or not
14. sudo lsof -i :8080 -> which application using the particular port 8080.
15. sudo su -> kill pid -> then you can kill the application runing.
16. mvn package -> mvn not installed or if you see "not found error" then its not installed
17. not enough space -> du -sh -> occupied size
18. df -kh  -> will show the detailed space occupied by the application detailed view.
19. permision denied -> its because of "service" or "filefolder" permisions provided ex-> docker is permision denied is kind of service issue.(user -aG).


